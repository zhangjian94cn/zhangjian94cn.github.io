---
title: " Reinforcement Learning from Human Feedback (RLHF)"
layout: post
author: "Zhang Jian"
header-style: text
mathjax: true
tags:
  - rlhf
---

### 2023 03 27, Monday, 10:58:42

今天先基于ray做一下吧，搞清楚使用ray的整个流程
1. 学习下rllib的example
2. 还是得进一步搞清楚trlx的实现，再在ray上进行实现
   1. ppo在ray上的配置：[ray-ppo](https://docs.ray.io/en/latest/rllib/rllib-algorithms.html#proximal-policy-optimization-ppo)

        ```python
        from ray.rllib.algorithms.ppo import PPOConfig
        config = PPOConfig()  
        config = config.training(gamma=0.9, lr=0.01, kl_coeff=0.3)  
        config = config.resources(num_gpus=0)  
        config = config.rollouts(num_rollout_workers=4)  
        print(config.to_dict())  
        # Build a Algorithm object from the config and run 1 training iteration.
        algo = config.build(env="CartPole-v1")  
        algo.train()  
        ```
   2. [trlx的实现流程](https://github.com/CarperAI/trlx/blob/main/examples/summarize_rlhf/trlx_gptj_text_summarization.py)
      1. sft model, rm model
      2. config train (epochs, batch_size, pipeline, trainer), model, 

3. 实现步骤
   1. 





## April












