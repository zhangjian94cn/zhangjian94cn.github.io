---
title: "2023 February"
layout: post
author: "Zhang Jian"
header-style: text
mathjax: true
tags:
  - work
  - weekly
---

## Week 05

### Summary

1. designed a detailed realization for current group sharing method
2. added GroupAGG and re-implemented NodeGroup
3. supported yuliang to reproduced the speedup result


### how to share

目前，已有的结果是：**group的sharing关系**，例如第1，3，8个group可以共享（完全不考虑group所在的层级以及所对应的tree）

需要搞清楚的是：**用于share的data sample，怎么存，如何使用**。有以下几种方案：

1. lazy mode: 同一个set中的group第一次被使用到时，才去进行gather
2. eager mode: 对所有的set都预先进行gather

这两种方案，都有些问题。lazy mode可能会出现多线程对同一块内存的写入，导致性能下降；eager mode即使在分享率达到10的情况下，仍然会消耗大量的gather，原因在于：**在第二层的16个group中，只有一个group会被真正使用到，如果直接对这第二层的所有group都预先进行gather，那么将会出现大量的浪费**

所以，我们应当**尽可能的对确切会被使用到的group进行share**，之前无论是lazy mode还是eager mode，思考的前提都是：**对所有group进行share**。而现在，我们应当修改这一假设，即：**尽可能的对被使用到的group进行share**。

因此，对当前的目标实现进行限制：
> 1）**对所有tree第一层的group进行share** 


### where to save

从尽可能的提升sharing rate的角度来思考，那么应当尽可能的保证：**能share的group在同一个数据结构中**，然而从之前的sharing打印结果来看，share的分布非常不平衡，例如第一个set可能包含了200多，而有的可能只有3，4个，**这很有可能会不利于多线程的负载均衡**。因此，我假设：

> 2）**保证每个set都包含相同数目的group** 

目前，准备直接按照排列顺序进行划分，考虑到训练过程中，**loss的下降具有一定连续性**，连续的boost tree会包含相类似的feature

### realization


some experiments
```cpp
#include <iostream>
#include <vector>
using namespace std;

class Node {};

// 
class NodeGroup : Node{
public:
    ~NodeGroup() = default;

    void load(std::vector<float> fval, std::vector<uint8_t> fidx) {};

    uint8_t next(float data) {
        // return static_cast<DataType*>(this)->nextImpl(data);
        cout << "float" << endl;
        return 0;
    }
    
    uint8_t next(int data) {
        // return static_cast<DataType*>(this)->nextImpl(data);
        cout << "int" << endl;
        return 0;
    }
};

template <>
class NodeGroup<int>{
    uint8_t next(int data) {
        cout << "int" << endl;
        return 1;
    }
};


template <>
class NodeGroup<double>{
    uint8_t next(double data) {
        cout << "double" << endl;
        return 1;
    }
};


int main(){
    vector<Node*> c;
    NodeGroup<int>    a = NodeGroup<int>();
    NodeGroup<double> b = NodeGroup<double>();
    c.push_back(&a);
    c.push_back(&b);
    return 0;
}
```

对predict不同输入进行函数重载

```cpp
class NodeGroup : Base {
public:
    using RegisterType = __m512; 
    
    ~NodeGroup() = default;

    void load(std::vector<float>& fval, std::vector<uint8_t>& fidx);

    inline uint8_t next(float* data);

    inline uint8_t next(RegisterType& data);

private:
    FeatIdxType _fidx;
    FeatValType _fval;
    std::vector<int8_t> _children; 
};


class RegTree {
public:
    inline float predict(const float* smpArr, RegisterType& data);

    void load(std::vector<float>& fval, std::vector<uint8_t>& fidx);

private:
    std::vector<NodeGroup> _groups;
    std::vector<float> _leaf;
}

class TreeAgg {
public:
    
    inline predict();

private:
    std::vector<RegTree> _trees;
    RegisterType r;
}
```

### concluison

1. load group sharing result
2. test code and optimize

## Week 06

### summary

1. Confirmed TreeAgg Class Interaction between Python and C++
   1. c++ invoke python to load TreeAgg data
   2. Tree Sharing Operation Reimplemented in C++
   3. use python to generate json model and c++ load it 

2. Completed Model Definition and Generation in Python
   1. just share first layer group
   2. output json model, including tree id, sharing feature value and index 

3. WIP: Modified Model Definition and Loading in C++

### Interface between C++ and Python

#### C++ invoke python

1. add `#include <Python.h>`
2. use c++ to invoke python function 
    ```c++
    PyObject* pModule = PyImport_ImportModule("<module_name>");
    PyObject* pFunc = PyObject_GetAttrString(pModule, "<function_name>");
    PyObject* pResult = PyObject_CallObject(pFunc, NULL);

    if (PyInt_Check(pResult)) {
        int result = PyInt_AsLong(pResult);
        // process result
    } else if (PyFloat_Check(pResult)) {
        double result = PyFloat_AsDouble(pResult);
        // process result
    } else if (PyString_Check(pResult)) {
        char* result = PyString_AsString(pResult);
        // process result
    }
    ```




发现这种方式不适合去加载比较复杂的类型

#### reimplemented analysis use cpp

之前，我们的模型加载方式是将xgboost json转化为group tree，如果我们需要加上分析过程，需要1）直接load初始tree model，2）对其进行分析，3）再将分析结果转化为group tree model

use xgboost to load tree model 

```cpp
#include <iostream>
#include <xgboost/c_api.h>

void load(string path) {
// Load the model from a JSON file
BoosterHandle handle;
XGBoostLoadModelFromJson(path, &handle);

// Get the number of trees in the model
int num_trees;
XGBoostGetAttr(handle, "num_trees", &num_trees);
std::cout << "Number of trees: " << num_trees << std::endl;

// Clean up
XGBoostFreeBooster(handle);
}
```

### define TreeAgg json model

在c++和python之间通过定义好的json file来交互，继续使用python来及进行sharing的分析，c++端加载相应json模型。此外，考虑到后续的非连续的共享以及上周的一些实现假设，需要进行以下修改

1. 保存group对应的树的索引
2. 只对第一层的group进行共享
3. 对设定好的agg_num个tree进行共享

目前python端的主要步骤：

```python
# dump trees
trees = booster_native.get_dump(dump_format="json")

# load sub_trees
tree_agg_num = 10
for i in range(math.ceil(len(trees)/tree_agg_num)):
    cur_idx = i * tree_agg_num
    groups_node = []
    groups_root = []
    for tree in trees[cur_idx : cur_idx + tree_agg_num]:
        sub_roots, sub_nodes = get_subtrees(json.loads(tree), 8, 3)
        groups_node.append(sub_nodes)
        groups_root.append(sub_roots)

    # make node share
    feat_set, gid_set = make_group_set(groups_node, cur_idx)

    # save
    groups_root = [x[0] for x in groups_root]
    convert_tree_agg(groups_root, feat_set, gid_set)
```

python侧的模型转化（`convert_tree_agg`）

```python
import json
from collections import deque

class TreeAgg:
    def __init__(self, roots, reg, idx):
        self.roots = roots
        self.reg = [list(x) for x in reg]
        self.idx = idx

def binary_tree_to_dict(root):
    
    tree = {'weight': [], 'index': []}
    queue = deque([root])

    while queue:
        node = queue.popleft()
        if 'leaf' not in node.keys():
            tree['weight'].append(node['split_condition'])
            tree['index'].append( int(node['split'][1:]))

            for child in node['children']:
                queue.append(child)

        else:
            tree['weight'].append(node['leaf'])
            tree['index'].append(0)
        
    return tree


def tree_agg_to_dict(tree_agg):
    
    tree_agg_dict = {
        'reg': list(tree_agg.reg),
        'idx': tree_agg.idx,
        'trees': []}

    for root in tree_agg.roots:
        tree_agg_dict['trees'].append(binary_tree_to_dict(root))
    
    return tree_agg_dict

def save_tree_agg(tree_dict, filename):
    with open(filename, 'w') as f:
        f.write(json.dumps(tree_dict))

def convert_tree_agg(groups_root, feat_set, gid_set):

    tree_agg = TreeAgg(groups_root, feat_set, gid_set)
    tree_agg_dict = tree_agg_to_dict(tree_agg)
    save_tree_agg(tree_agg_dict, 'test.json')

```

![](/img/20230209165540.png)  


### c++ loading

在c++端，主要增加以下内容

![](/img/20230209170251.png)  

补充完善了上周group_tree_acc的实现，进度：load one treeagg json

![](/img/20230209171022.png)  


