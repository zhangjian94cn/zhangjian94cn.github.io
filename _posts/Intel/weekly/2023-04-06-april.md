---
title: "2023 April"
layout: post
author: "Zhang Jian"
header-style: text
mathjax: true
tags:
  - work
  - weekly
---

## WEEK 14

### summary

1. conducted a preliminary study of the rllib
2. figure out the implementation of rlhf in rllib

### rllib

![](/img/rllib-table1.png)

强化学习算法在其创建的计算模式中是高度不规则的,突破了如今流行的分布式框架所支持的计算模型的界限。这种不规则发生在如下几个层面:

1. 根据算法的不同，任务的持续时间和资源需求也有数量级的差异；例如 A3C的更新可能需要几毫秒，但其他算法如 PPO 需要更大粒度时间颗粒。
2. 通信模式各异，从同步到异步的梯度优化，再到在高通量的异策略学习算法（如 Ape-X 和 IMPALA）中拥有多种类型的异步任务，通信模式各不相同。
3. 基于模型的混合算法、强化学习或深度学习训练相结合的超参数调优、或是在单一算法中结合无导数优化和基于梯度的优化等方式产生嵌套计算
4. 强化学习算法经常需要维护和更新大量的状态，包括策略参数、重放缓冲区，甚至还有外部模拟器等。

现有的计算框架（如 Spark、MPI）通常是假设有规律的计算模式，当子任务的持续时间、资源需求或嵌套不同时，这些计算框架会有性能损失

### align rllib and gpt rlhf


1. custom env (LLMEnv)
   1. action space (词表的大小)
   2. observation space
   3. return reward (reward model)
2. PPO Algorithm
   1. custom model (ActorCritic model)
   2. config parameter



```python
class LLMEnv(gym.Env):
    def __init__(self, env_config):
        # pick actual env based on worker and env indexes
        self.env = gym.make(...)
        self.action_space = self.env.action_space
        self.observation_space = self.env.observation_space
    def reset(self):
        return self.env.reset()
    def step(self, action):
        return self.env.step(action)

register_env("LLMEnv", lambda config: LLMEnv(config))
```

`step` - Updates an environment with **actions** returning the next agent **observation**, the **reward** for taking that actions,


```python
class ActorCritic(TorchModelV2, nn.Module):

    def __init__(self, obs_space, action_space, num_outputs, model_config, name):
        TorchModelV2.__init__(self, obs_space, action_space, num_outputs, model_config, name)
        nn.Module.__init__(self)

        self.actor = ActorModel()
        self.critic = CriticModel()
        
        # self._feature = None
    
    def forward(self, input_dict, state, seq_lens):
        obs = input_dict["obs"].float()
        action_logits = self.actor.forward(self._feature)
        return action_logits, state
    
    def value_function(self):
        return self.critic.forward(self._feature)

ModelCatalog.register_custom_model("ac_model", ActorCritic)
```

config ppo

```python
config = (
    ppo.PPOConfig()
    .environment(
        LLMEnv,
        env_config={},
    )
    .training(
        gamma=0.99,
        entropy_coeff=0.001,
        vf_loss_coeff=1e-5,
        model={
            "custom_model": "ac_model",
            "custom_model_config": {
            # ...
            },
        },
    )
    # .framework(args.framework)
    .rollouts(num_envs_per_worker=1)
    .resources(
        # Use GPUs iff `RLLIB_NUM_GPUS` env var set to > 0.
        num_gpus=int(os.environ.get("RLLIB_NUM_GPUS", 0))
    )
)
```

目前已经清楚的是：RLlib与custom model (ActorModel, CriticModel, Reward Model)的关系，存在的问题是：数据流的细节（从输入prompt开始，generate seq，replay buffer，sample training batch，train step）

#### PPO Pipeline

PPO_Algorithm -> Algorithm -> Worker_Set -> Rollout_Worker -> Policy (build_policy_map) -> Env

1. **Algorithms** contain a **WorkerSet** under `self.workers`. A WorkerSet is normally composed of a single local worker (self.workers.local_worker())
2. **WorkerSet** is fault tolerant and elastic. It tracks health states for all the managed remote worker actors.
3. Each worker (remotes or local) contains a **PolicyMap**, which itself may contain either one policy for single-agent training or one or more policies for multi-agent training.
4. **RolloutWorker**. This class wraps a **policy** instance and an **environment** class to collect experiences from the environment.


###  NEXT

1. 搞清楚从env中获取observation的具体方式
<!-- 2. 如何将rollout的结果存到buffer中 -->


## WEEK 15

1. convert language processing to RL format
   1. how to set observation space?
   2. add `class ExamplesSampler` for env
2. implemented a micro PPO example whose action is a single token.
   1. run successfully
   2. dummy forward data 

### observation space

1. 在rllib中追溯env与actor的交互流程
   1. algorithm.py
   2. rollout_worker.py
   3. sampler.py: item = next(self._env_runner)
   4. env_runner.py: gym.env返回observation, reward...

    ![](/img/Sample-collection-process.png)

2. 研究并尝试了gym中可以作为llm observation space的结构
   1. MultiDiscrete
   2. Tuple
   3. Sequence
   4. Text

    https://gymnasium.farama.org/api/spaces/composite/



### prompts sampler

之前困惑我时间较长的一个问题是：env中除了提供observation，action，还应该有什么

```python
class LLMEnv(gym.Env):
    def __init__(self, env_config):

        # set token param
        self.max_tokens = 1024
        self.cur_tokens = 0

        self.tokenizer = tokenizer
        self.action_space = Discrete(self.tokenizer.vocab_size)
        # self.action_space = MultiDiscrete(np.array([self.tokenizer.vocab_size] * self.max_tokens))
        self.observation_space = MultiDiscrete(np.array([self.tokenizer.vocab_size] * self.max_tokens))
        # self.observation_space = Sequence(Discrete(self.tokenizer.vocab_size))

        self.context = np.array([self.tokenizer.eos_token_id] * self.max_tokens)
        self.reward = RewardModel()
```

目前的想法是：env中还应该提供prompts，即**构建一个向agent提问的环境**，在每次reset时，应当选取新的prompts

```python
def reset(self, *, seed=None, options=None):
    # self.context = [self.tokenizer.eos_token_id] * self.max_tokens
    self.context = np.array([self.tokenizer.eos_token_id] * self.max_tokens)
    
    txt_inputs = self.example_sampler.sample(1)
    tok_inputs = tokenizer(
        txt_inputs, padding=True, return_tensors="np", truncation=True
    )
    prompts = tok_inputs["input_ids"].squeeze()
    # self.context
    self.cur_tokens = prompts.shape[0]
    self.context[:self.cur_tokens] = prompts
    return self.context, {}
```

### current pipeline

目前的主要流程如下：

1. env reset: get prompts
2. actor_critic model 
   1. actor **generate** seq and forward
   2. critic calc value
3. env step
   1. calc reward
   2. return **experience**

和chatgpt中的流程区别在于：seq generate，training batch，replay buffer，

hf已经提供了seq的generate接口，如果按照目前方案one action one step的方式，generate (experience) 的时间消耗会比较大 （prompts repeat calc）

```python
sequences = self.model.generate(
    input_ids=states,
    attention_mask=state_mask,
    temperature=temperature,
    max_new_tokens=max_completion,
    no_repeat_ngram_size=3,
    # max_length=1024
)
```

step的observation返回 要求和init的space一致，不能在一个环境中得到一个batch；

需要将generate的experience 保存于replay buffer中

## WEEK 16

### SUMMARY

1. Solved actor_critic model foward problem where action is converted to 0/1 and it is incorrect.
2. Read the PR code and compare it with mine, which gives me some inspiration.
   1. framework is similar and details have many difference.
   2. found a problem and I have commented.
3. Run the new RLHF code successfully and debug into the detail.

### actor_critic model foward problem

when debugging the forward function, I found that parameter action is converted to 0/1 strangely.

By tracking the code, I found that multidiscret space is preprocessed to onehot format in default. As a result, I define a custom gym space for action.

<!-- ![](/img/gym-space-onehot-preprocess.png) -->

```python
class TokenSpace(Space[np.ndarray]):
    def __init__(
        self, 
        nvec: Union[np.ndarray, list], 
        dtype=np.int64, 
        seed: Optional[Union[int, np.random.Generator]] = None):
        
        self.nvec = np.array(nvec, dtype=dtype, copy=True)
        assert (self.nvec > 0).all(), "nvec (counts) have to be positive"

        super().__init__(self.nvec.shape, dtype, seed)

    # ...
```


### similarities and differences

1. LLMEnv
    1. RM (same)
    2. SFT model (ChatLLama DOESN'T Have)
    3. Dataset (Also add dataset to env)
    4. Action_space (Dict, Repeat, Discrete)
    5. Observation_space (Dict, Repeat, Discrete)

2. Buffer ()

3. PPORLHF Algorithm
    1. RLHFSampler
    2. PPORLHF

4. RLHF_PPO_Module
     1. Actor
     2. Critic (forget to add map layer)
     3. _forward_train

5. RLHF_PPO_Torch_Learner
     1. compute_loss_per_module

---

**It use some rllib's new features in RLHF**
1. Catalog API
2. RLModule API

---

if we are doing sequences in 1 shot, how are we gonna take discounted future return into consideration? should the reward function score every single token generated? [link](https://github.com/ray-project/ray/pull/34168/files/327e139aed49b157426ffee9543fb93c6184e2e4#r1160962386)

With reference to other implemtations, I found that it should add a configurable discount to the reward.

```python
gamma = self.config.trainer.gamma_discounted
discounted_rewards = torch.zeros_like(old_values)
for i in range(discounted_rewards.shape[1]):
    for j in range(i, discounted_rewards.shape[1]):
        discounted_rewards[:, i] += (
            gamma ** (j - i) * rewards[:, j]
        )
```

---

**build from source, solved some env problem abd run current code successfully**

### Next

1. debug into detail 


## WEEK 17


1. fix bug in PPO training pipeline
   1. solved tokenizer dimension bug

2. train sft, rm and PPO based on huggingface trainer 
   1. higher transformer version: param sync error, downgrade
   2. poor parallelism: always use 1 thread; 1 iter needs 100+s (batch size=1, max_generate_token=8)
   3. training of sft is correct, but rm model still has some problems. As a result, reward is nan currently.

3. training larger model in PPO pipeline is a challenge
      1. cpu utilization is pool
      2. a large amount of time is used in ram (gpt2 is much better)

---

### huggingface trainer

目前：gpt-j-6使用huggingface trainer的效率，1024 token，batch size 4

```bash
Number of trials: 1/1 (1 RUNNING)
+--------------------------------+----------+--------------------+--------+------------------+--------+-----------------+-----------+
| Trial name                     | status   | loc                |   iter |   total time (s) |   loss |   learning_rate |     epoch |
|--------------------------------+----------+--------------------+--------+------------------+--------+-----------------+-----------|
| HuggingFaceTrainer_62ca2_00000 | RUNNING  | 10.0.0.156:1958411 |    131 |          10433.1 | 0.4495 |     4.99714e-06 | 0.0017179 |
+--------------------------------+----------+--------------------+--------+------------------+--------+-----------------+-----------+
```
![](/img/sft-train-cpu-usage.png)

training log

![](/img/sft-train.png)


### ppo training


```bash
# gpt2 * 4
Number of trials: 1/1 (1 RUNNING)
+-----------------------------+----------+-------------------+--------+------------------+------+----------+----------------------+----------------------+--------------------+
| Trial name                  | status   | loc               |   iter |   total time (s) |   ts |   reward |   episode_reward_max |   episode_reward_min |   episode_len_mean |
|-----------------------------+----------+-------------------+--------+------------------+------+----------+----------------------+----------------------+--------------------|
| PPORLHF_RLHFEnv_c71a4_00000 | RUNNING  | 10.0.0.156:329247 |      8 |           428.75 |   16 |      nan |                  nan |                  nan |                nan |
+-----------------------------+----------+-------------------+--------+------------------+------+----------+----------------------+----------------------+--------------------+

perf:
cpu_util_percent: 40.82142857142858 
ram_util_percent: 6.602380952380954  

# gpt-j-6 * 4

+-----------------------------+----------+--------------------+--------+------------------+------+----------+----------------------+----------------------+--------------------+
| Trial name                  | status   | loc                |   iter |   total time (s) |   ts |   reward |   episode_reward_max |   episode_reward_min |   episode_len_mean |
|-----------------------------+----------+--------------------+--------+------------------+------+----------+----------------------+----------------------+--------------------|
| PPORLHF_RLHFEnv_29309_00000 | RUNNING  | 10.0.0.156:1576953 |      1 |          801.734 |    2 |      nan |                  nan |                  nan |                nan |
+-----------------------------+----------+--------------------+--------+------------------+------+----------+----------------------+----------------------+--------------------+

perf:
cpu_util_percent: 16.47381615598886
ram_util_percent: 88.36114206128134    
```


### next

1. solved RM training problem
   1. using ray debug, solved loss.backward() bug. (loss=0, param config problem)
   <!-- 2. modify eval dataset size for faster  -->
   3. sovled the lack of `compute_loss_context_manager()` func.
2. continue testing ppo training
   1. reward still nan
   <!-- 2. start -->




```bash
+--------------------------------+----------+--------------------+--------+------------------+--------+-----------------+-----------+
| Trial name                     | status   | loc                |   iter |   total time (s) |   loss |   learning_rate |     epoch |
|--------------------------------+----------+--------------------+--------+------------------+--------+-----------------+-----------|
| HuggingFaceTrainer_23069_00000 | RUNNING  | 10.0.0.156:2875654 |     73 |          20970.5 | 0.8706 |     4.99681e-06 | 0.0019146 |
+--------------------------------+----------+--------------------+--------+------------------+--------+-----------------+-----------+

```

![](/img/rm-train.png)

/home/zhangjian/doc/ray/python/ray/rllib/examples/custom_eval.py